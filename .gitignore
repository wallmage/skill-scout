.DS_Store
__pycache__/
*.pyc

# Internal process records — plans, specs, optimizer runs, eval fixtures.
# Kept locally, never published.
planning/

# Scratch task prompts written for coding agents.
gpt-fix-prompt*.md

# Development-only test suite. The skill runs without it, so it is not
# published. Kept locally for verifying changes before release.
tests/
